apiVersion: v1
kind: Service
metadata:
  name: podverse-api
spec:
  type: ClusterIP
  selector:
    app: podverse-api
  ports:
    - name: http
      port: 1234
      targetPort: 1234
      protocol: TCP
